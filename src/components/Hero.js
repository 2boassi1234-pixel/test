import React from 'react';
import '../shop.css';

const Hero = () => {
    return (
        <section className="hero-banner">
            <div className="hero-image-wrapper">
                <div className="hero-image-container">
                    {/* Hero decorative elements can be added here if needed */}
                </div>
            </div>
        </section>
    );
};

export default Hero;

